# 🗣️ Speaker Counter

ミーティングやグループワークで、メンバーごとの発言回数を簡単に記録・集計できるWebアプリケーションです。

発言回数を+-ボタンを用いてカウントし、集計ボタンを押すだけでランキング形式で結果を表示します。議論への参加状況を可視化し、発言の偏りを把握することを目的として制作しました。

**【アップデート】**
手動でのリアルタイムカウント機能に加え、**Discordの特定のテキストチャンネルからトーク履歴を自動抽出し、発言回数を自動集計するバックエンド連携機能**を新たに搭載しました。

---

## 主な機能

* メンバーの入力欄を自由に追加・削除
* ボタン操作による発言回数のカウント
* 発言回数順のランキング表示
* 同率順位に対応したランキング
* 🥇🥈🥉による上位3位の表示
* 入力内容をまとめてリセット
* PC・スマートフォン両対応のレスポンシブデザイン
* **【新規】Discord自動集計機能**: チャンネルIDを入力するだけで、対象の部屋のトーク履歴（最大100件）から発言回数を自動カウント

---

## 画面イメージ

![Speaker Counter](SpeakerCounter_image.png)

---

## デモ

GitHub Pages

https://harukahida.github.io/SpeakerCounter/

*(※セキュリティおよびAPI通信の仕様上、Discord自動集計機能は以下の「ローカルでの起動方法」に沿ってサーバーを起動した環境で動作します)*

---

## 使用技術

* HTML5
* CSS3

  * Flexbox
  * Media Query
* JavaScript (Vanilla JS)
* Google Material Icons
* Node.js (v18+) ➔ バックエンド（APIサーバー）の構築
* Express ➔ APIエンドポイントの作成
* discord.js ➔ Discord APIとの通信、メッセージ履歴の取得
* dotenv / cors ➔ トークン（秘密の鍵）の安全な管理、CORS対策

---

## 💡 工夫したポイント

### レスポンシブデザイン

PCでは入力画面と集計結果を左右2カラムで表示し、スマートフォンでは縦1カラムへ自動で切り替わるレイアウトを採用しました。画面サイズに応じて見やすく操作しやすいUIを実現しています。

### 操作ミスを防ぐUI

カウントが0のときはマイナスボタンを自動的に無効化し、負の値になることを防止しています。また、新しい入力欄を追加すると自動でカーソルが移動するため、キーボードだけでスムーズに入力できます。

### 同率順位への対応

集計結果は発言回数の降順でソートし、同じ回数の場合は同じ順位を表示するよう実装しました。順位のずれが発生しないランキングアルゴリズムを作成しています。

### 【新規】セキュリティを考慮したバックエンドの分離

Discord APIを利用するためのBotトークン（パスワード）をフロントエンドのJavaScriptに直接記述すると、ブラウザ側から誰でも鍵を盗めてしまう重大な脆弱性が発生します。
そこで、Node.js（Express）による独立したバックエンドサーバーを中継させ、`.env`ファイルを用いてトークンをサーバー側に完全に隠蔽・安全管理する設計にしました。

---

## 🔥 苦労した点

入力欄を追加・削除できる仕様のため、削除後に新しい入力欄を追加するとIDが重複し、ボタン操作や集計結果が正しく動作しない問題が発生しました。

そこで、

* 削除しても番号が重複しないグローバルカウンターを導入
* 集計時にはIDではなく画面上のDOM要素を直接取得する方式へ変更

という設計に変更しました。
これにより、入力欄を何度追加・削除しても安定して動作するようになりました。

---

## ローカルでの起動方法（セットアップ）

### 1. クローンとライブラリのインストール
```bash
git clone [https://github.com/HarukaHida/SpeakerCounter.git](https://github.com/HarukaHida/SpeakerCounter.git)
cd SpeakerCounter
npm install
```

### 2. 環境変数の設定
ルートディレクトリに .env ファイルを新規作成し、ご自身のDiscord Botトークンを設定してください。

```env
DISCORD_TOKEN=あなたのDiscord_Botトークン
PORT=3000
```
(※Botには「MESSAGE CONTENT INTENT」の権限を付与し、対象のサーバーに招待しておいてください)

### 3. バックエンドサーバーの起動
```bash
node server.js
```
サーバー起動後、index.html をブラウザで開くことで、手動入力およびDiscord同期機能がすべて利用可能になります。

---

## ディレクトリ構成

```text
SpeakerCounter/
├── index.html            # フロントエンド画面
├── style.css             # デザインCSS
├── count.js              # フロントエンド処理（DOM操作・API通信）
├── server.js             # 【新規】Node.js/Expressによるバックエンド
├── package.json          # 【新規】依存ライブラリ一覧
├── package-lock.json     # 【新規】ライブラリのバージョン詳細記録
├── .gitignore            # 【更新】秘密の鍵(.env)などをGit管理から除外
├── README.md             # 本ドキュメント
├── LICENSE               # MITライセンス
└── SpeakerCounter_image.png
```

---

## 今後追加したい機能

* 発言割合のグラフ表示
* CSV形式で結果をダウンロード
* 発言履歴の保存
* チャットアプリケーションとの連携

  * チャット履歴を抽出し、発言回数を自動カウント

---

## ライセンス

MIT License
